# Local Mosquitto broker for the edge gateway
# Plaintext only — the plug and edge service are on the same LAN

listener 1883
allow_anonymous false
password_file /mosquitto/config/passwd

persistence true
persistence_location /mosquitto/data/

log_dest stdout
log_type warning
log_type error
log_type notice
